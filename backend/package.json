{
  "name": "zentara-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Zentara backend — minimal rebuild (Express + node:sqlite + zod).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "zod": "^3.24.0"
  }
}
